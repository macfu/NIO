### 解释一下BIO,NIO,AIO的区别
```
   BIO: 同步阻塞的IO操作，一个线程只可以管理一个连接，而后所有的操作在进行处理的时候都有可能读取或返回额时候u阻塞状态
   NIO: 同步非阻塞IO操作，这里面实现最大的缺点就是总要去判断Buffer中是否有数据
   AIO: 异步诶租测IO操作，最大的特点是在于回调处理，而对于AIO的实现是从JDK1.7之后开始提供
   [click me](http://www.google.com)
```